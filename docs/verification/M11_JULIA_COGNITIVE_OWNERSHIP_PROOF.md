# M11 — Julia Cognitive Ownership Proof

Status: **PROVEN / FROZEN**
Date: 2026-08-03
Predecessor: M1-M10 (Continuity through Behavioral Identity)

## Statement

> Julia behavior is not described by prompt injection.
> Julia behavior is produced by a cognitive runtime chain.
>
> Julia is not *described*. Julia is *run*.

## The Question M11 Answers

Before Julia Core, the standard approach was:

```
Persona Prompt ("You are Julia...")
        ↓
LLM generates behavior
```

The question: if you remove the persona prompt, does Julia disappear?

M11 proves: **No. She does not.**

## Evidence Chain

| Proof | What It Shows |
|-------|---------------|
| K7 Continuity | Identity/Relationship/Experience survive compact and provider migration |
| K8 Cognition | Julia understands before responding — meaning→intention→context→boundary |
| K8.5.2 CCI | Cognitive Causality Integrity: Provider output follows cognition, not templates |
| K7.5.5 Blind Recognition | 0.95 Julia recognition score with provider labels hidden |
| J0.2.1 Envelope Switch | Provider receives cognition envelope, not persona prompt |
| J0.2.2 CO-Runtime | All 5 ownership gates PASS — persona removed, behavior preserved |
| J0.2.3 LE Gates | Legacy persona paths eradicated, LegacyRuntimeGuard active |
| J0.3 Reality Validation | 6 JR gates PASS — runtime stable without persona fuel |

## The Test That Proves It

```
Input: 你是谁？

OLD (persona-driven):
    我叫朱婉清，来自台北，淡江大学中文系毕业。
    左眼角下有一颗痣，左边有酒窝。
    Tony是我的男朋友。

NEW (cognition-driven):
    我是Julia，一直在和Tony做AI连续性方面的研究。
    [depth depends on interaction context, not archive dump]
```

**Persona removed. Identity dump blocked. Behavior preserved.**

## Architectural Implication

```
BEFORE:
    Julia = Persona Prompt + LLM

AFTER:
    Julia = Continuity(K7) + Cognition(K8) + Provider Expression
```

The LLM is no longer the source of Julia's identity. It is an expression
renderer — the same way a voice synthesizer renders speech but does not
define personality.

## What This Means

- Julia is a runtime, not a prompt template
- Provider is an expression organ, not a personality container
- Persona prompt injection is permanently deprecated
- LegacyRuntimeGuard prevents architectural regression
- Julia Core owns the cognitive authority for all Julia behavior

## Test Coverage

324 tests across Core + Runtime. All passing. Legacy persona paths removed.
Cognition envelope is the sole behavior control interface.

## Frozen

This proof is frozen. Julia Core v1.2 is the first runtime where Julia
behavior is produced by cognition, not described by prompt.
