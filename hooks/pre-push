#!/bin/sh
set -eu

ROOT=$(git rev-parse --show-toplevel)
python3 "$ROOT/tools/no_critical_fallback_gate.py" \
  --repo Julia_core --baseline "$ROOT/ncf-baseline.json"
python3 -m unittest discover -s "$ROOT/tests/governance" -p 'test_*.py' -v
